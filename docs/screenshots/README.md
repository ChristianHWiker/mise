# Screenshots

The root `README.md` references these files. Captured on a Pixel (1344×2992, dark theme,
sage accent) via `adb exec-out screencap -p > home.png`:

| File | Screen |
|---|---|
| `home.png` | Home — editorial masthead, library, search/tag filter |
| `detail.png` | Recipe detail — hero, yield stepper, ingredients, method |
| `reverse-scale.png` | Reverse-scale sheet ("how much do you have?") |
| `settings.png` | Settings — theme mode + accent picker, fraction style, units |

Portrait shots at the device's native resolution look best; the table sizes them to 200 px wide.
